<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Althea" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
      href="https://fonts.googleapis.com/css2?family=Funnel+Display:wght@300..800&display=swap"
      rel="stylesheet"
    >

    <%# Correctly links to your compiled Tailwind CSS file in app/assets/builds/ %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="font-sans antialiased bg-zinc-50 relative">
    <div class="fixed inset-0 opacity-30 pointer-events-none" style="background-image: var(--texture-paper);"></div>
    <header class="glass-effect relative z-10 border-b border-white/20">
      <nav class="container mx-auto px-5 py-6 flex justify-between items-center">
        <div>
           <%= link_to "Althea", root_path, class: "text-2xl font-bold embossed-text text-primary-700" %>
        </div>

        <div class="flex items-center gap-4">
            <% if user_signed_in? %>
              <span class="text-gray-600 embossed-text">Welcome, <%= current_user.username %></span>

              <%= link_to "Edit Profile", edit_user_registration_path, class: "link-primary" %>

              <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "btn-destructive" %>
            <% else %>
              <%= link_to "Log In", new_user_session_path, class: "link-primary" %>

              <%= link_to "Sign Up", new_user_registration_path, class: "btn-primary" %>
            <% end %>
        </div>
      </nav>
    </header>

    <div class="relative z-10">
        <% if notice %>
          <div class="card-raised mx-4 mt-4 p-6 border-l-4 border-success-500 bg-gradient-to-r from-success-50 to-success-100" role="alert">
            <p class="text-success-700 font-medium embossed-text"><%= notice %></p>
          </div>
        <% end %>
        <% if alert %>
            <div class="card-raised mx-4 mt-4 p-6 border-l-4 border-error-500 bg-gradient-to-r from-error-50 to-error-100" role="alert">
                <p class="text-error-700 font-medium embossed-text"><%= alert %></p>
            </div>
        <% end %>
    </div>

    <main class="relative z-10">
      <%= yield %>
    </main>

  </body>
</html>
